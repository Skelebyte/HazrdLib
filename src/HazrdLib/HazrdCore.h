#pragma once

#include <stdio.h>
// #include "../ext/sdl/SDL.h"

// static inline int ininitalize() {

//     if(SDL_Init(SDL_INIT_EVERYTHING) > 0) {
//         printf("Failed to initalize SDL!");
//         return 1;
//     }

//     return 0;
// }

// static inline int ininitalizeCustom(Uint32 customFlags) {
//     if(SDL_Init(customFlags) > 0) {
//         printf("Failed to initalize SDL!");
//         return 1;
//     }

//     return 0;
// }